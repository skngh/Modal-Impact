## Release

envelope value goes from sustain to 0 in x amount of seconds. If looping, this will get trigger once loop is set to false. If not looping, the release stage gets automatically triggered after the decay stage.

**Note**: RTPC's will have no effect once event is started

Units: s <br/>
