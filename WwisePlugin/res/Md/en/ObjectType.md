## Object Type

Selects modal bank preset. Each preset has 15 modes in them, that were hand tuned mimicing real life objects. Sets the base character of the plugin. Can be used nicely with switch containers to determine which preset to use on collision.
