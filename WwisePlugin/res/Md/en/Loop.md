## Loop

When enabled, the envelope will trigger the attack then decay stage, and loop at whatever the sustain level is set at. Release phase is trigger once loop is unchecked. Used for things like scraping sounds, then set the loop RTPC to false to release it.
