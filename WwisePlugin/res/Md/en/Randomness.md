## Randomness

Amount of random variation applied to the preset's mode frequencies, gains and
decay times each time the sound starts. At 0 every impact is identical. Higher
values give each hit its own character, which helps avoid repetition.

Units: normalized <br/>
